import React, { Component, PropTypes } from 'react';
// import { AppBar } from 'react-toolbox/lib/app_bar';
require('./app.scss');

const App = () => (
  <div className="app">
    <h1>Hello Arnaud</h1>
  </div>
);

export default App;
